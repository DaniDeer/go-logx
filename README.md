# GO Logx - A Go logging library with zero allocations and structured logging

Another logging library in GO, because I found myself re-writing logging helpers in my GO projects permanently.
Logging should be simple, so this library uses GO builtin packages of `GO 1.26`.
